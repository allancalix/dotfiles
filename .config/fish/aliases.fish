# General
abbr -a e $EDITOR

# Cargo
abbr -a c 'cargo'
abbr -a cr 'cargo run'
abbr -a cb 'cargo build'
abbr -a ct 'cargo test'

# Kubernetes
abbr -a k 'kubectl'
abbr -a kg 'kubectl get'

# Terraform
abbr -a tf 'terraform'
abbr -a tfa 'terraform apply'
abbr -a tfp 'terraform plan'

# Tmux
abbr -a tsw 'tmux switch -t'

# Tmuxinator
abbr -a t2s 'tmuxinator start'
abbr -a t2x 'tmuxinator stop'
abbr -a t2l 'tmuxinator list'
